
-- Events: --                   -- Listeners: --

ActionEvent                     ActionListener (buttons)

ItemEvent                       ItemListener (list items)

WindowEvent                     WindowListener (window / JFrame)

KeyEvent                        KeyListener (keyboard)

MouseEvent                      MouseListener (mouse)
